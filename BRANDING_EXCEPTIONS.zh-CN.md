# 品牌例外清单

**简体中文** | [English](BRANDING_EXCEPTIONS.md)

> 非官方参考译文，发生歧义时以英文原文为准

YiQiao 是面向用户的产品名称。本清单记录暂时保留的继承标识，因为更改这些标识会破坏
Python 导入、已存储数据、配置或集成。将某项内容列入本清单，并不表示允许新增上游品牌标识。

## 1. Python 兼容命名空间

YiQiao 的正式 Python 导入入口为：

```python
from yiqiao import Memory, AsyncMemory
```

可安装发行包名为 `yiqiao-memory`。继承实现的源代码目录和 `mem0` 导入命名空间仍作为
兼容层保留，因为现有扩展会导入该命名空间下的多个模块；直接删除会破坏调用方、序列化
类型路径、插件和内部导入。新代码和新文档必须使用 `yiqiao`，兼容入口不属于 YiQiao
公开 API 承诺。

由兼容命名空间衍生的继承类名和变量名，例如 `Mem0ValidationError`，可以继续保留在内部
代码中。它们不得作为产品名称出现在界面文案、API 描述或新文档中。

## 2. 配置与本地状态

记忆核心和服务端仍会识别旧版环境变量前缀，以及由使用者显式指定的本地状态路径，
使现有安装能够在不丢失配置的情况下完成迁移。新部署和新用法默认将状态存储在
`~/.yiqiao`，如需覆盖目录，必须使用 `YIQIAO_DIR`。保留的兼容范围包括：

- `MEM0_DIR` 和 `MEM0_API_KEY` 作为显式兼容别名。仅当未设置 `YIQIAO_DIR` 时才会
  读取 `MEM0_DIR`。两个变量均未设置时，全新安装默认使用 `~/.yiqiao`；如果该目录
  尚不存在但已有 `~/.mem0`，YiQiao 会继续使用旧目录，避免升级后静默隐藏历史记录
  或身份状态。创建 `~/.yiqiao` 或显式设置 `YIQIAO_DIR` 即可选择规范路径。
- `scripts/import_chat_history.py` 会在对应的 `YIQIAO_*` 变量之后，兼容读取
  `MEM0_BASE_URL`、`MEM0_USER_ID` 和 `MEM0_AGENT_ID`。这些命令行兼容入口可让现有的
  无人值守导入任务迁移，同时避免静默更改其目标地址或实体范围。
- `MEM0_TELEMETRY`、`MEM0_TELEMETRY_SAMPLE_RATE` 和
  `MEM0_TELEMETRY_STATE_PATH`。YiQiao 自有的遥测设置优先级更高，并且遥测默认关闭。
- `MEM0_LLM_*`、`MEM0_EMBEDDER_*`、`MEM0_EMBEDDING_*`、
  `MEM0_RERANK_*` 和 `MEM0_DEFAULT_*` 等服务端配置系列。

新的部署级设置必须使用 `YIQIAO_` 前缀。只有在替代方案已发布至少一个版本，并且迁移指南
同时覆盖无人值守命令行任务和服务端配置之后，旧版别名才能在有明确说明的破坏性版本中移除。

## 3. 持久化与协议标识

如果重命名会导致指标分裂、重复导入记忆或使现有数据无法读取，则包含 `mem0` 的内部遥测
事件名称、迁移集合名称、确定性 UUID 命名空间和已存储配置键可以保留。遥测默认关闭。
这些标识不属于公开品牌界面，所有新标识都必须使用 YiQiao 命名。

`mem0/configs/vector_stores/*.py` 中公开的向量存储配置结构，仍保留继承的默认集合、
数据库、表、键空间、索引和命名空间值，例如 `mem0` 和 `mem0_db`。对应的持久化前缀和
实现默认值仍位于 `mem0/vector_stores/chroma.py`、`cassandra.py`、
`databricks.py`、`langchain.py`、`neptune_analytics.py`、`redis.py` 和
`valkey.py` 中。现有库用户的数据可能只存储在这些隐式位置下；直接更改这些值会使数据看似
丢失，或创建第二份存储。对于新的标准部署，YiQiao 服务端配置会使用自己的 `memories`
集合；新的自定义集成应选择与 YiQiao 品牌无冲突的显式位置标识。只有在某个主版本能够检测
旧存储、提供经过测试的复制或重命名迁移，并在迁移期间支持回滚或双读时，才能移除这些继承
默认值。

第三方兼容 API 所要求的现有协议请求头或 SDK 字段，只能保留在相应的兼容路由中。
YiQiao 原生端点、Webhook、请求头和示例必须使用与 YiQiao 品牌无冲突的名称或标准名称。

## 4. 法律与历史归属

对 Mem0 和 `mem0ai/mem0` 的引用会保留在 `LICENSE`、`NOTICE`、本文件、第三方声明、
源代码历史以及明确标注的上游归属说明中。这些引用用于标明衍生作品的来源，不表示任何赞助
或认可关系。

## 5. 密钥扫描配置

YiQiao 的公开发布历史是干净的源代码快照，不保留上游仓库中与具体提交绑定的密钥扫描指纹。
发布树中不存在 `.gitleaksignore`；当前代码树和每一个公开的 YiQiao 提交都必须在没有指纹
例外的情况下通过扫描。

`.gitleaks.toml` 仅在检测文本恰好为相邻且值为空的 `OPENAI_API_KEY=` 和
`ANTHROPIC_API_KEY=` 条目时允许匹配。它不会将某个路径加入允许列表，因此环境变量模板的
其余部分仍会接受每一条规则的扫描；只要为其中任一变量填入值，该表达式便不再匹配。
此配置不包含密钥值、代码摘录、作者姓名、电子邮箱地址或产品文案。

## 不属于例外的内容

下列内容不属于兼容性要求，不得出现在 YiQiao 的发布体验中：

- 上游云服务注册、控制台、MCP、文档、演示、Discord、社交媒体、支持邮箱、软件下载、
  筹款或营销链接。
- 上游徽标、徽章、截图、分析密钥或托管服务调用。
- 页面标题、导航、API 摘要、镜像名称、Compose 服务、默认标签或新示例中的上游产品名称。
- 以任何上游软件包或组织名称发布 YiQiao 构件。

任何新增例外都必须说明具体兼容原因、受影响的文件或接口、移除条件，并经过维护者审核。
